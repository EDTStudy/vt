
&НаКлиенте
Процедура ЗаполнитьТаблицу(Команда)
	сч = 2;
	алфавит = "аАбвгдеёжзклм";   
	Пока сч<10 цикл           
		сч = сч + 1;
		НоваяСтрока = Таблица.Добавить();
		НоваяСтрока.Имя = Сред(алфавит,сч,1);
		НоваяСтрока.Номер = сч;
	КонецЦикла;
КонецПроцедуры
